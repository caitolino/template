# template
This has my html template, and maybe more!

git remote set-url origin git@github.com:caitolino/template.git<br/>
git pull --rebase origin main<br/>
git push --set-upstream origin main<br/>
git push
